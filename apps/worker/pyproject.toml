[project]
name = "f1-ingest-worker"
version = "0.1.0"
description = "FastAPI and RQ worker for FastF1 ingestion"
requires-python = ">=3.11"
dependencies = [
  "fastf1",
  "fastapi",
  "pandas",
  "redis",
  "requests",
  "rq",
  "uvicorn",
]

[tool.uv]
package = false
